# sigsleep

```
USAGE: sigsleep [options] -- <command> [arguments ...]

sigsleep - wrapper command to proxy signals to command process and inject sleep
           around command execution.

Options:
    -after=n     Sleep n seconds after the command exits.
```
